# RL REMEDIATION MATRIX

**Date:** 2026-09-08
**Purpose:** Map every audit finding to its RL-relevant fix before implementation

---

| # | Finding | Severity | Current State | RL-Relevant Fix | Files Affected | Verification |
|---|---------|----------|---------------|-----------------|----------------|--------------|
| 1 | **Broken GRPO reward** — `train.py:reward_fn()` does not use ground truth. Rewards any valid format equally regardless of correctness. | **CRITICAL** | reward_fn checks format only (valid action string, valid intent string, reasoning length). Maximum 0.65 for any output. Cannot distinguish correct from incorrect. | Rewrite GRPO reward to use `RewardCalculator` with scenario ground truth. GRPOTrainer passes `**reward_kwargs` including extra dataset columns — add `scenario_id` to dataset, build prompt→scenario lookup in reward closure. | `scripts/train.py`, dataset preparation | Unit test: correct output scores > incorrect output for same scenario. Degenerate "always ASK" policy scores < 0.6. |
| 2 | **No ground-truth correctness in GRPO** — GRPO path cannot compare model output to ideal action/intent/missing info. | **CRITICAL** | GRPO dataset has only `prompt` column. No scenario_id, no ground truth linkage. | Add `scenario_id` column to GRPO dataset. Build `scenario_map` dict in reward function closure. Reward function receives `prompts, completions, scenario_id` (via reward_kwargs) and uses RewardCalculator. | `scripts/train.py` | Print reward breakdown for first 5 training completions — verify intent/action/missing_info components are non-zero and vary. |
| 3 | **ASK_CLARIFICATION exploit** — "always ask" strategy scores ~0.55-0.65 composite reward because ASK is ideal/acceptable in ~85% of scenarios. | **HIGH** | Action distribution: ASK 71-80% across splits. R_efficiency weight only 0.10, not enough to penalize. | Two fixes: (1) Rebalance dataset — add scenarios where SEARCH_KB, PROVIDE_STEP, ROUTE_CLAIM, ESCALATE, COMPLETE are ideal actions. Target ~30-40% ASK, not 80%. (2) Increase R_efficiency weight or add explicit penalty for action monotonicity. | `scripts/generate_scenarios.py`, `src/rewards/components.py`, `configs/reward_weights.yaml`, regenerate data | Run degenerate policy gate: ALWAYS_ASK must score < strong baseline. |
| 4 | **No trained-model evaluator** — `run_baselines.py` evaluates baselines only. No script loads trained LoRA adapter and evaluates on test set. | **HIGH** | No `evaluate_trained.py` exists. | Create unified `scripts/evaluate.py` that loads any model condition (base, base+prompt, base+LoRA) and evaluates on any dataset split. | New `scripts/evaluate.py` | Run on test set for both baseline and trained model. Outputs go to `results/`. |
| 5 | **Temperature/config mismatch** — Baselines use temp=0.3, training uses temp=0.7-0.8, trained evaluation temp undefined. | **MEDIUM** | Confounded comparison. | Set ALL evaluation to temp=0.3. Training generation can use higher temp (0.7) for diversity. Document explicitly. | `scripts/evaluate.py`, `scripts/run_baselines.py` | Grep codebase: every eval call uses temp=0.3. |
| 6 | **Missing per-class metrics** — Only overall action accuracy reported. With 80% ASK in test, overall accuracy is misleading. | **HIGH** | `metrics.py` has action_counts but no per-class precision/recall/F1, no confusion matrix. | Add confusion matrix, per-action precision/recall/F1, macro-F1, action entropy. | `src/evaluation/metrics.py` | Test: compute_metrics returns confusion_matrix, macro_f1 keys. |
| 7 | **Missing confusion matrix** — Cannot visualize whether model collapses to one action. | **HIGH** | Not implemented. | Add to metrics.py. Output as both dict and printable table. | `src/evaluation/metrics.py` | Confusion matrix printed to terminal after every evaluation run. |
| 8 | **No real experiment results** — Zero baselines run, zero training, zero evaluation. | **CRITICAL** | Empty `results/` directory. | Run full pipeline: generate data → validate → baselines → RL training → evaluation → comparison → report. | All scripts, `results/` | `results/final_comparison.json` exists with real numbers. |
| 9 | **No generalization evidence** — All test scenarios from same template generator. | **MEDIUM** | Test set is template variants only. | Create `data/challenge/` with 20-30 hand-crafted novel scenarios: paraphrased, multi-issue, angry, contradictory, already-tried-steps. Evaluate separately. | New `scripts/generate_challenge.py`, `data/challenge/` | Challenge set evaluation produces separate metrics. |
| 10 | **PyTorch 2.2.2 incompatible with transformers 5.16.1** — TRL GRPOTrainer cannot import due to `nn` NameError in transformers. | **CRITICAL** | `from trl import GRPOTrainer` crashes. | Downgrade to `transformers==4.46.3` + `trl==0.15.2` which are compatible with PyTorch 2.2.2. Pin versions in requirements.txt. | `requirements.txt`, pip install | `python3.11 -c "from trl import GRPOTrainer; print('OK')"` succeeds. |
| 11 | **ESCALATE and COMPLETE never ideal** — Two of six actions exist only as wrong answers. No scenario has them as ground truth. | **MEDIUM** | 0% of scenarios use ESCALATE or COMPLETE as ideal_action. | Add scenarios: safety-concern escalations, fully-resolved completions, repeated-contact completions. | `scripts/generate_scenarios.py` | Each action appears as ideal_action in at least 5% of train and test. |
| 12 | **No .gitignore** — Model weights, cache, pycache would be committed. | **LOW** | Missing file. | Create .gitignore. | New `.gitignore` | `git status` shows clean after adding .gitignore. |
| 13 | **No README** — Repo has no documentation. | **MEDIUM** | Empty. | Create after experiments with real results. | New `README.md` | README includes: problem, method, results table, reproduce commands. |
| 14 | **No reward hacking test suite** — No automated tests that degenerate policies score poorly. | **HIGH** | Only 36 unit tests for components. No integration tests for degenerate strategies. | Add `tests/test_degenerate.py` with ALWAYS_ASK, ALWAYS_ESCALATE, ALWAYS_COMPLETE, RANDOM policies. Verify all score below engineered baseline. | New `tests/test_degenerate.py` | All degenerate policies score < 0.5 composite reward on balanced dataset. |
| 15 | **R_clarification keyword matching exploitable** — Word-level overlap allows keyword stuffing. | **MEDIUM** | `topic_words.update(t.split("_"))` allows single-word matches. | Tighten: require full topic match (not individual words). Or weight by specificity. Accept this as documented limitation for MVP. | `src/rewards/components.py` | Document in REWARD_AUDIT. Keyword stuffing detection in failure analysis. |
